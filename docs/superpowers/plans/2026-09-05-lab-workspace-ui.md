# 实验室工作台前端实施清单

> 使用 executing-plans 在当前仓库逐项执行。用户已批准直接开发，按其要求保持计划简短，文档工程后置。

**Goal:** 完成现代实验室工作台美化及已发现的响应式缺陷修复。

**Architecture:** 保留现有 Vue 页面和后端接口，公共布局与组件负责视觉一致性；功能入口从授权菜单生成，业务表单保留原页面职责。

**Tech Stack:** Vue 3、Element Plus、Pinia、Vite、Vitest、真实浏览器 Playwright CLI。

---

- [x] 公共体验：修改 src/assets/styles、layout、Hamburger、Pagination、RightToolbar，修复手机表格固定列、筛选宽度、导航拥挤；用实际 390 像素失败截图作为布局回归基线，并为分页行为补针对性测试。
- [x] 功能发现：新增授权路由入口工具及对应测试，再接入首页和仪表盘；测试覆盖嵌套路由、查询参数、隐藏路由与外链，不生成未授权入口。
- [x] 业务页面：在 src/views/lab 统一标题、筛选、表格、详情、弹窗和消息交互，保留全部业务操作与权限指令。
- [x] 登录：修改 src/views/login.vue，统一品牌、可访问标签和验证码错误处理，不改变登录与安全策略。
- [x] 每个逻辑增量验证并提交；最终在 ruoyi-ui 运行 npm test 和 npm run build:prod，预期测试通过、构建成功。
- [x] 真实浏览器复验全部实验室页面入口，抽查六类角色；在 320/390/768/1024/1440 宽度验证代表性页面，检查控制台、请求和键盘操作，截图保存在 output/playwright。
- [x] 审阅差异、确认未提交本地凭据及 .vscode、推送已验证提交，交付页面链接和实际验证范围。

## 本轮实际交付与验证（2026-09-05）

- 代码提交：`b113772`（主题、登录、公共布局与响应式业务页）、`d50eb90`（授权功能入口与消息反馈），已推送 `origin/main`。
- 最终前端验证：`npm test` 15 个测试文件、92 项全部通过；`npm run build:prod` 成功。保留现有构建体积警告，本轮不扩展为性能专项。
- 使用本机真实后端、MySQL 和 Redis，六类演示账号均经真实登录；检查全部实验室页面入口，抽查设备/预约/维修详情、巡检执行、隐患详情及相关表单。没有为视觉测试提交新的业务记录。
- 代表性视口覆盖 320、390、768、1024、1440；复验手机固定列遮挡、表单与描述布局、菜单搜索的 Tab/Enter 操作、主题切换与按钮对比度。学生预约空表单准确提示三项必填错误，设备选项来自真实接口且没有代办学生字段。
- 最终工作台重新加载后控制台 0 errors / 0 warnings，所见认证、路由、工作台和未读消息请求均为 HTTP 200。登录校验负例产生过预期失败响应，未将整个长会话描述为从无错误。
- 前后截图仅保存在本地 `output/playwright`，包括 `workspace-final-desktop.png`、`login-desktop.png`、`login-dark-final.png`、`student-apply-validation.png`、`device-detail-mobile.png`、`hazard-timeline-mobile-final.png`；凭据、截图、运行产物和 `.vscode` 未纳入提交。
- 验证边界：本轮不是全部业务写操作的完整 E2E，也没有覆盖所有浏览器、真机、弱网组合或完整无障碍审计；系统管理页面仅作代表性抽查。已发现并纳入本轮的 UI 缺陷已修复，不承诺穷尽所有条件的零缺陷。
